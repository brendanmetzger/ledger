var cycle = function (index, limit) {
  return function () {
    return index++ % limit;
  }
};


function Validator(url) {

  // https://validator.nu/?doc=http://iam.colum.edu/students/Zachary.Knibbs/SWM/&out=json&callback=process
}
